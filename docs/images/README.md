# Imagens da documentação

Ficheiros referenciados pelo `README.md` na raiz do repositório.

| Ficheiro | Conteúdo |
|----------|----------|
| `readme-report-overview-2026.png` | Relatório **Allure Report 3** — vista principal (Awesome). |
| `readme-report-graphs-2026.png` | **Graphs** — gráficos e tendências (incl. histórico quando `KEEP_HISTORY` + `history.jsonl`). |
| `readme-report-timeline-2026.png` | **Timeline** — execução por host/processos. |
| `readme-swagger-2026.png` | **Swagger UI** deste serviço (lista de endpoints). |
| `flow-01-swagger-post-projects.png` | Swagger — `POST /projects` (criar projeto). |
| `flow-02-swagger-get-projects.png` | Swagger — `GET /projects` (listar). |
| `flow-03-swagger-get-project-by-id.png` | Swagger — `GET /projects/{id}` (detalhe). |

Para atualizar: gere novas capturas no teu ambiente, substitui os ficheiros (mantém os nomes ou ajusta as referências no `README.md`) e faz commit.
